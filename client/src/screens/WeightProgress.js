import { View, Text } from "react-native";
import React from "react";

const WeightProgress = ({ route }) => {
  return (
    <View>
      <Text>WeightProgress</Text>
    </View>
  );
};

export default WeightProgress;
