import { View, Text } from "react-native";
import React from "react";

const AddFood = ({ route }) => {
  return (
    <View
      style={{
        backgroundColor: "#aaa",
        marginTop: -10,
        flex: 1,
        paddingTop: 10,
      }}
    >
      <Text>AddFood</Text>
    </View>
  );
};

export default AddFood;
